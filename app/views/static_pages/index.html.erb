<% unless logged_in? %>
<p><%= link_to "新規登録", signup_path, class: "btn btn-primary" %>
 <%= link_to "ログイン", login_path, class: "btn btn-primary" %></p>
<% end %>

<%= render 'head' %>


<% if @micropost_items.any? %>
  <ol class="microposts">
    <%= render @micropost_items %>
  </ol>
  <%= will_paginate @micropost_items %>

<% end %>