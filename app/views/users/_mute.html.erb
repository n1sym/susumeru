<%= form_for(current_user.active_relationship_mutes.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit "ミュート", class: "btn btn-primary " %>
<% end %>